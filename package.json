{
  "name": "mezoyield",
  "version": "0.0.1",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20.0.0 <23.0.0"
  },
  "private": true,
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "scripts": {
    "dev": "pnpm --filter @mezoyield/app dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint"
  },
  "pnpm": {
    "overrides": {
      "chai": "^4.5.0"
    }
  }
}
